# ============================================================
# 本地开发 Profile（I26）
# 启用：--spring.profiles.active=dev
# ============================================================

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  show-actuator: false

info:
  app:
    description: B2B2C 多租户电商 API（I1–I27 · dev · OpenAPI）
